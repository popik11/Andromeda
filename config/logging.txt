### Флаги конфигурации логирования (флаги совместимости вверху)

## Дублировать логи админов/приватных действий в LOG_CATEGORY_GAME для старых парсеров логов
LOGGING_COMPAT_ADMINPRIVATE

## Дублировать записи логов в человекочитаемом формате
LOG_AS_HUMAN_READABLE

### Основные флаги логирования

## Логирование входа/выхода игроков
LOG_ACCESS

## Логи действий администраторов
LOG_ADMIN

## Логи админ-чата
LOG_ADMINCHAT

## Логи отладки ассетов (закомментировано)
# LOG_ASSET

## Логи атак
LOG_ATTACK

## Логи экономических действий
LOG_ECON

## Логи эмоций
LOG_EMOTE

## Логи опросов призраков
LOG_GHOST_POLL

## Логи игровых событий (начало раунда, результаты и т.д.)
LOG_GAME

## Логи отладки распределения профессий (закомментировано)
# LOG_JOB_DEBUG

## Логи манифеста экипажа в отдельный файл
LOG_MANIFEST

## Логи действий мехов
LOG_MECHA

## Логи OOC-чата
LOG_OOC

## Логи сообщений КПК
LOG_PDA

## Логирование изображений
LOG_PICTURES

## Логи молитв
LOG_PRAYER

## Логи устной речи (Say)
LOG_SAY

## Логи действий шаттлов
LOG_SHUTTLE

## Логи действий ИИ и киборгов
LOG_SILICON

## Логи речевых индикаторов
LOG_SPEECH_INDICATORS

## Логи подозрительных входов (бан, подделка CID и т.д.) в отдельный файл
LOG_SUSPICIOUS_LOGIN

## Логи телекоммуникаций
LOG_TELECOMMS

## Логи сброса таймеров (закомментировано)
# LOG_TIMERS_ON_BUCKET_RESET

## Логи взаимодействий инструментов [/atom/proc/tool_act]
LOG_TOOLS

## Логи задач предателей
LOG_TRAITOR

## Включение функции twitterize() для конвертации текста в твиты (закомментировано)
## Пример "использования": https://twitter.com/Poly_the_Parrot
# LOG_TWITTER

## Логи покупок в аплинке
LOG_UPLINK

## Логи вирусов и действий с ними
LOG_VIRUS

## Логи голосований
LOG_VOTE

## Логи шёпота (Whisper)
LOG_WHISPER

## Логи всех вызовов world.Topic()
LOG_WORLD_TOPIC

## Логи ручного переключения зон атаки
LOG_ZONE_SWITCH
