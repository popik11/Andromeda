# Внешние ресурсы
# Укажите здесь расположение .zip-архива с ресурсами сервера (.rsc внутри).
# Если указать несколько ссылок, сервер будет чередовать их.
# Для работы необходимо отключить предзагрузку ресурсов в BYOND (компиляция с PRELOAD_RSC=0).
# Ссылки должны быть без шифрования (http://), так как загрузка идет через BYOND, а не браузер.

EXTERNAL_RSC_URLS http://tgstation13.download/byond/tgstationv2.zip


########################
# Настройки ассетов   #
########################
# Ассеты - файлы интерфейсов: CSS, изображения, JavaScript и т.д.
# Эти настройки определяют, как клиенты получают эти файлы.

# Способ доставки ассетов
# По умолчанию используется встроенная система BYOND, но это может быть медленно.
# Альтернатива - сохранение ассетов в веб-доступную папку (например, на CDN).
# Допустимые значения: simple (по умолчанию), webroot.
#ASSET_TRANSPORT webroot


# Настройки для способа 'simple'

# Раскомментируйте для фоновой отправки всех ассетов клиентам заранее
# (вместо загрузки по требованию). Рекомендуется для production-серверов.
#ASSET_SIMPLE_PRELOAD


# Настройки для способа 'webroot'

# Локальная папка для сохранения ассетов
# Формат имен: asset.ХЕШ.РАСШИРЕНИЕ или namespaces/хеш/ИМЯ_ФАЙЛА
#ASSET_CDN_WEBROOT data/asset-store/

# URL для доступа к этой папке
# Веб-сервер должен поддерживать длительное кеширование (ассеты используют хеш-URL).
# HTTPS поддерживается, но Linux-клиенты могут иметь проблемы с TLS > 1.0.
# Для тестирования можно использовать скрипт localhost-asset-webroot-server.py
#ASSET_CDN_URL http://localhost:58715/

# Веб-сервер должен включать CORS-заголовки для работы TGUI. Пример для IIS:
# <add name="Access-Control-Allow-Origin" value="*" />
# <add name="Vary" value="Origin" />
# <add name="Access-Control-Allow-Methods" value="GET, POST, OPTIONS" />
# <add name="Access-Control-Max-Age" value="0" />
